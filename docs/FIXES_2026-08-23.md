# Fixes 2026-08-23 — Full Offline + Gerakan/Senjata/Map/Gameplay

**Build:** `npm run typecheck --workspace=client` 0 error, `npm run build --workspace=client` OK `index 300k / three 754k`

## 1. Online 5v5 Dihapus → Full Offline
- `client/src/App.tsx:1` hapus `MultiplayerMode` + `/play`, hanya `/training /offline5v5 /zombie /l4d`
- `client/src/screens/MainMenu.tsx:10` `ModeId = training|zombie|offline5v5|l4d` (4 card), hapus `ServerBrowser`/`MatchLobbySetup`
- `client/src/stores/useGameStore.ts:4` `GameMode` hapus `multiplayer`
- `client/src/stores/useNetworkStore.ts` + `useZombieNetworkStore.ts` + `hooks/useNetwork.ts` + `config/network.ts` + `game/network/ServerPredictionManager.ts` → **stub offline no-op** (compat)
- `components/KOTH/ReconnectOverlay/ServerBrowser/VoteKick` dihapus, `game/player/RemotePlayers.tsx` diganti `RemoteBots` pakai `useOffline5v5Store`
- `client/package.json` hapus `colyseus.js`, root `package.json` `dev` tanpa `server`

## 2. Dark Screen offline5v5 Fixed
- **Penyebab:** `Offline5v5Mode.tsx` Canvas tanpa `background/fog`, `PlayerController` spawn di `(0,0)` dalam `Mid Box`, `RemotePlayers` import error → crash → hitam
- **Fix:** Baru `Offline5v5Mode.tsx` (281 baris) — `background #0e1520 + fog 30-90 + ambient 0.55 + directional 1.2`, `Physics` + `ContainerYard`, `PlayerController` spawn `SPAWN.T`, `RemoteBots` via `useOffline5v5Store`, `OfflineLoop` fixed `1/60` accumulator, HUD top `T score - Round - CT` + `MENU [ESC]`

## 3. Gerakan
- `ZombieArcadeController.tsx:1` baru `useAimStore` (bukan `window.__zombieAim`), `BOUNDS` per mode (`training ±15`, `zombie ±59`, `l4d ±35/42`, `container ±29`), `yaw lerp 14`, `camera lerp 8`, `setLastInput` untuk spread
- `PlayerController.tsx:54` `MODE_BOUNDS` hapus `multiplayer` tambah `l4d`, spawn offline `TRAINING_ARENA`/`SPAWN.T`, `wall jump` enable untuk `offline5v5|training`, `effectiveIsDead` baca `Offline5v5Store`, `sendInput` → `setLocalPos` offline only
- `usePlayerInput.ts` tetap, `INPUT_WINDOW_MS` untuk jump buffer

## 4. Senjata
- `ShootingSystem.tsx:1` `isZombieArcade()` = `zombie|l4d`, `getArcadeAim()` via `useAimStore`, hapus `sendShoot/sendMelee` network, `zombieEngine.handleMelee` + L4D cone `0.78` head 18%, `grenade` no network
- `WeaponModel.tsx` PMREM refCount, `useWeaponStore.ts` switch `offline5v5` via `localSwitchWeapon` (stubbed), `ReloadSystem` stub
- `ZombieEngine.ts:281` + `handleMelee(dir)` cone `0.55` 2.5m 65 dmg

## 5. Map START→FINISH Modular
- `L4DCampaignMap.tsx:1` SafeRoom `0,-36 (14×14 hijau)` → Corridor `6×len` walls → `cp1 -18 → cp2 8,0 (circle 6) → cp3 18 → Finale 36*scale` Rescue `circle 8 heli/boat`, visual only (arcade bounds clamp)
- `ZombieSurvivalMode.tsx` ground `120` + corridor `8×70` + **START ring `0,-30`** + **FINISH circle `0,30`** + 6 barricades `b_start 0,-25 … b_finish 0,25` planks
- `ContainerYard.tsx` tetap `MAP_OBSTACLES` AABB, `TrainingArena` ±15

## 6. Gameplay Loop
- `Offline5v5Store.tick` fixed `1/60` accumulator (anti drift)
- `ZombieEngine` `SpatialGrid 5` O(1), `InstancedMesh MAX100` 1 draw, `PowerUp 5%`, `wave_active` START→FINISH
- `L4DDirector.ts:1` `BuildUp 35s → Sustain 20s → Relief 18s`, horde cap `42`, `special <3`, `horde drip 0.08` (5/s), witch/tank `0.002`
- `L4DMode.tsx` 4 survivors (1 you +3 bot follow `3.4 m/s`), bot shoot nearest <18m, progress `(z+36)/72`, finale `call 4s → holdout 40s (vehicle 20s) → escape 10s <8m`

## 7. Dokumen Update (2026-08-23 Offline v4.0)
- `README.md` quick start tanpa server, tabel 4 mode START→FINISH, tech stack offline, struktur folder baru
- `Game_Design_Document.md` v4.0 offline, 4 modes, fixed tick, START→FINISH
- `docs/README.md` banner offline, link `Zombie_Shooter_System_v1.md`, status 4 mode
- `Design_Player.md` v2.1 dua controller, `Design_Weapons.md` v2.2 offline shooting, `Design_Gameplay.md` v2.1 4 mode, `GAMEPLAY_SYSTEM.md` v3.1 offline, `Design_Networking_Advanced.md` LEGACY stub, `Analysis_Reference_Doc.md` v2.1 offline, `ZOMBIE_MODE.md` offline v1.0, `IMPROVEMENTS_AND_FIXES_AUDIT.md` P0.3 & P1.2 fixed, `Zombie_Shooter_System_v1.md` single source
- Baru: `docs/FIXES_2026-08-23.md` (ini), `docs/Zombie_Shooter_System_v1.md` (14 bab), `L4D` via `useL4DStore`/`L4DDirector`

## 8. Verification
```
npm run typecheck --workspace=client  # 0 error
npm run build --workspace=client      # 300k + 754k
npm run build --workspace=shared      # ok
```
Test: `/training` aim/recoil, `/offline5v5` 1+9 bot + buy/plant, `/zombie` wave + START/FINISH, `/l4d` 4 ch Director horde
